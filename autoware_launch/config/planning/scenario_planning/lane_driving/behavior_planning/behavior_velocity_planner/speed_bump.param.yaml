/**:
  ros__parameters:
    speed_bump:
      slow_start_margin: 0.5
      slow_end_margin: 0.0
      print_debug_info: true

      # limits for speed bump height and slow down speed to create a linear equation
      speed_calculation:
        min_height: 0.05 # [m]
        max_height: 0.15 # [m]
        min_speed: 3.2 # [m/s] = [5 kph]
        max_speed: 3.4  # [m/s] = [10 kph]
